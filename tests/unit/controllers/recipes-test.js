import {
  moduleFor,
  test
} from 'ember-qunit';

import Ember from 'ember';

moduleFor('controller:recipes', 'RecipesController', {});

test('it exists', function() {
  var controller = this.subject();
  ok(controller);
});
